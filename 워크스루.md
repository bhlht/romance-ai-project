# AI 로맨스 소설 프로젝트 - 워크스루 (사용 설명서)

## 개요
우리는 **계층적 생성 시스템(소설 공장)**, **심층 검토 및 자동 수정**, 그리고 **표지 제작(Imagen 3)** 기능을 성공적으로 구현했습니다.

## 주요 기능 가이드

### 1. 소설 공장 (Novel Factory - Batch Generation)
**목표**: 클릭 한 번으로 50화(약 300페이지) 분량의 소설을 완성합니다.
- **사용 방법**:
    1.  **Novel Factory (Batch)** 탭으로 이동합니다.
    2.  **Start Production (300 Pages)** 버튼을 클릭합니다.
    3.  시스템이 다음 작업을 자동으로 수행합니다:
        -   **기획 (Plan)**: Gemini Pro가 50화 플롯을 짭니다.
        -   **집필 (Write)**: DeepSeek가 각 챕터를 이어서 씁니다.
        -   **검토 (Review)**: Gemini가 각 챕터의 품질을 체크합니다.
        -   **기억 (Track)**: `MemoryManager`가 줄거리와 설정을 기억합니다.
    4.  "Monitor Job" 섹션에서 실시간 진행 상황을 지켜보세요.
    5.  **☑️ 자동 이어붙이기 (Auto-Merge)**: 이 옵션을 켜두면, 작업이 끝난 후 완성된 소설이 **자동으로 `Current Story`에 입력**됩니다. (바로 Editor's Desk에서 검토 가능!)

### 2. 심층 검토 및 자동 수정 (Editor's Desk)
**목표**: AI 편집자에게 피드백을 받고, 마음에 안 드는 문장을 자동으로 고칩니다.
- **사용 방법**:
    1.  **Editor's Desk** 탭으로 이동합니다.
    2.  "Story Engine" 탭에 글이 작성되어 있어야 합니다.
    3.  **Run Deep Analysis** 버튼을 클릭합니다.
    4.  **개연성**, **문법**, **창의성** 점수와 구체적인 피드백을 확인합니다.
    5.  **✨ AI Auto-Fix (자동 수정)**: **Generate Fix** 버튼을 누르면, AI가 피드백을 반영해 문장을 다시 써줍니다.
    6.  **비교 검토**: [원본]과 [수정본]을 나란히 비교해 보세요.
    7.  **적용/재시도**: 
        -   마음에 들면 `✅ Accept`를 눌러 본문에 적용합니다.
        -   마음에 안 들면 `🔄 Regenerate`를 눌러 다시 고쳐오게 시킵니다.

### 3. 표지 제작소 (Art Studio)
**목표**: 소설 내용에 맞는 프로급 표지를 만듭니다.
- **사용 방법**:
    1.  **Art Studio** 탭으로 이동합니다.
    2.  **Generate Prompt from Story**를 눌러 소설 내용에서 표지 묘사를 추출합니다.
    3.  **Generate Image**를 눌러 **Imagen 3**로 고화질 표지를 생성합니다.

## 기술적 세부 사항
- **백엔드 (Backend)**:
    -   `memory_manager.py`: 장편 연재를 위한 RAG(검색 증강 생성) 기반 기억 관리.
    -   `main.py`: 장시간 소요되는 배치 작업을 비동기로 처리.
    -   `gemini_service.py`: Gemini Pro/Flash 및 Imagen 3 연동.
- **프론트엔드 (Frontend)**: `http://localhost:8000`으로 백엔드 API와 통신.

## 다음 단계
-   필요한 라이브러리 설치: `pip install -r frontend/requirements.txt` (`pandas`, `xlsxwriter` 포함).
-   백엔드 실행: `py -3.11 -m backend.main`
-   프론트엔드 실행: `py -3.11 -m streamlit run frontend/app.py`
